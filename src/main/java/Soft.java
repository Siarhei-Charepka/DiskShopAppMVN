import java.time.LocalDate;

public class Soft extends Disk {

    public Soft(DiskType diskType, String name, LocalDate inventoryDate, int rating) {
        super(diskType, name, inventoryDate, rating);
    }
}
